apiVersion: v1
kind: ConfigMap
metadata:
  name: microsim-landing-html
  namespace: microsim-dev
data:
  index.html: |
    <html>
      <head>
        <title>Microsim Observability Demo</title>
      </head>
      <body>
        <h1>Microsim Observability Demo</h1>
        <p>
          This endpoint is a static landing page.
        </p>
        <p>
          <strong>Microsim</strong> runs as a Kubernetes Job and generates
          distributed traces exported via OpenTelemetry.
        </p>
        <p>
          Please refer to Grafana / Jaeger dashboards to inspect traces,
          service dependencies, and latency distributions.
        </p>
      </body>
    </html>

